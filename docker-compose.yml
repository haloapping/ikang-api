services:
  database:
    container_name: ikang-db
    image: postgres:alpine
    ports:
      - "5432:5432"
    env_file:
      - path: .env
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
